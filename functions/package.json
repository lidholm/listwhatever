{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "test": "mocha --exit test/*.spec.js",
    "emulator:test": "firebase emulators:exec  --project=fakeproject  'npm run test'"
  },
  "engines": {
    "node": "16"
  },
  "dependencies": {
    "firebase-admin": "^9.12.0",
    "firebase-functions": "^3.20.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "firebase-functions-test": "^0.3.2",
    "firebase-tools": "^9.19.0",
    "mocha": "^8.0.1"
  },
  "private": true
}
